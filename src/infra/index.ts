/**
 * Infrastructure layer (adapters implementing ports).
 *
 * Concrete adapters: FlutterwaveGateway, S3ObjectStore, ProviderMailer,
 * BullMqScheduler, and Prisma repositories. These implement the interfaces in
 * `src/ports` and are wired into services at composition time.
 *
 * Populated by tasks 21-22.
 */
export {};
