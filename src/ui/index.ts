/**
 * UI layer (Tailwind component library).
 *
 * Accessible, responsive presentational primitives used by App Router pages
 * (Requirements 20). Components live here so presentation concerns stay
 * separate from the framework-agnostic service/domain layers.
 *
 * Populated by the presentation tasks.
 */
export {};
